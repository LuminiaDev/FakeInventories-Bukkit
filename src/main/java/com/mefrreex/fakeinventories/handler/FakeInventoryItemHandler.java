package com.mefrreex.fakeinventories.handler;

import org.bukkit.entity.Player;

public interface FakeInventoryItemHandler {
    void handle(Player player); // TODO more method parameters, slot, item, etc
}
